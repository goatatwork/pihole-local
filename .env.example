# Copy to .env (scripts/setup.sh does this for you) — .env is gitignored.
PIHOLE_PASSWORD=changeme
PIHOLE_TAG=latest
WEB_PORT=8089
TZ=UTC
# Semicolon-separated. Use your router (e.g. 192.168.2.1) if you need
# local hostname resolution; public resolvers avoid DNS loops.
UPSTREAMS="1.1.1.1;9.9.9.9"
